---

export interface Props {
    domainId: string;
  src?: string;
}

const { domainId, src } = Astro.props;
---

<!-- <script type="module" define:vars={{ domainId, src }}>
    import * as ackeeTracker from 'ackee-tracker';
    ackeeTracker.create(src).record(domainId);
</script> -->

<script
    async
    is:inline
    src={`${src}/tracker.js`}
    data-ackee-server={src}
    data-ackee-domain-id={domainId}
></script>

<script type="text/javascript">
    (function(c,l,a,r,i,t,y){
        c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
        t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
        y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
    })(window, document, "clarity", "script", "qex8eeeiqe");
</script>
