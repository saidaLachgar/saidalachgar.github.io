---

export interface Props {
  domain: string;
  src?: string;
}

const { domain, src = 'https://plausible.io/js/script.js' } = Astro.props;
---
<script is:inline src={src} data-domain={domain} defer></script>
