---
export interface Props {
  site: string;
  dataId: string;
  host: string;
}

const { dataId, site, host = '/umami.js' } = Astro.props;
---
<script is:inline defer src={`${site}${host}`} data-website-id={dataId}></script>
