---
title: "DevTools Tips to Boost Your Web Development"
summary: DevTools is a powerful set of tools for web development. Here are some handy tips to help you work more efficiently
date: '2024-11-02'
audio: 'dev-tools-tips-to-boost-your-web-development.mp3'
duration: '3m 50s'
tags: ['tips']
---
import './index.scss'
import Figcaption from "@/components/Figcaption.astro"
import img1 from "@/assets/img/simulate-pseudo-css-classes.png"
import img2a from "@/assets/img/select-elements-with-pointer-events-none.png"
import img2b from "@/assets/img/select-elements-with-pointer-events-none-shift.png"
import vid3 from "@/assets/videos/debug-js-events.webm"
import img4a from "@/assets/img/find-and-export-css-changes-firefox.png"
import img4b from "@/assets/img/find-and-export-css-changes-chrome-and-edge.png"
import img5 from "@/assets/img/test-slower-network-speeds.png"
import img6 from "@/assets/img/expand-dom-nodes.png"
import img7 from "@/assets/img/search-by-selector.png"
import img8 from "@/assets/img/scroll-to-element.png"
import img9 from "@/assets/img/drag-drop-nodes.png"


## Simulate Pseudo CSS Classes
<Figcaption src={img1} alt="Simulate Pseudo CSS Classes">Testing CSS pseudo-classes like `:hover` or `:active` can be tricky because you can't always use your mouse. In DevTools, you can easily simulate these states without any hassle. Simply inspect the element and click the **:hov** button to enable the desired pseudo-class.</Figcaption>

<br/>

<button type="button" class="demo-btn">try it here! ✨</button>

<br/><br/>
## Select Elements with `pointer-events:none`

<Figcaption src={img2a} alt="Can't select elements with pointer-events:none">Sometimes, you may need to select elements that have the `pointer-events:none` CSS property.</Figcaption>
 
<Figcaption src={img2b} alt="Select elements with pointer-events:none with shift">To do this, hold down the **Shift** key while hovering over the elements. This allows you to select them even if they are not normally clickable.</Figcaption>

<br/>

<button type="button" class="demo-btn untouchable">Am untouchable, am i ?</button>

<br/><br/>
## Setting Event Breakpoints

When dealing with events like `mouseover` and `mouseleave`, you can set breakpoints in the **Debugger** tab of DevTools to pause your code at the exact moment an event is triggered.

For example, if you have a dropdown menu that is created on `mouseover` and deleted on `mouseleave`, setting a breakpoint on the `mouseover` event allows yout to work on your menu without being bothered. 

The advantage here is that even if you **leave** the hoverable item, the menu won’t hide immediately because the browser pauses the execution. You can resume the code by clicking the **Play** button in the Debugger when you're ready to continue. 

<button 
    class="demo-btn" type="button" 
    onmouseover="this.insertAdjacentHTML('afterend', '<p>I appear using JS on `mouseover` Try to keep me in the debugger so u can style me ✨</p>')"
    onmouseout="this.nextSibling.remove()"
>Hover me 👀</button>

<br/>

<Figcaption asVideo src={vid3} alt="setting-event-breakpoints">Here is a demonstration :</Figcaption>

<br/><br/>

## Zoom the DevTools UI

If you find the DevTools interface too small, you can zoom in or out. Click inside the DevTools and use `Ctrl` + `+` to zoom in or `Ctrl` + `-` to zoom out and `Ctrl` + `0` to reset (use `Cmd` on a Mac).

## Find and Export CSS Changes

After modifying CSS in DevTools, you can save your changes. 

<Figcaption src={img4a} alt="find-and-export-css-changes-firefox">In Firefox, use the **Changes** panel to copy all edits.</Figcaption>

<Figcaption src={img4b} alt="find-and-export-css-changes-chrome-and-edge">In Chrome and Edge, access the **Changes** panel from the command menu by pressing `Ctrl` + `Shift` + `P`.</Figcaption>

<br/><br/><br/>
## Test Slower Network Speeds

<Figcaption src={img5} alt="test-slower-network-speeds">It's important to see how your site performs on slower connections. In the **Network** panel, select a throttling option from the dropdown to simulate a slow network speed. This helps you understand how users with slower connections experience your site for both api and <span title="Like google's LCP, FCP, CLS...">SEO score</span>.</Figcaption>

<br/><br/><br/>
## Expand DOM Nodes Recursively

<Figcaption src={img6} alt="Expand DOM nodes recursively">To save time while navigating the DOM tree, you can expand all child nodes of a selected element at once. Right-click on the node and choose **Expand Recursively** (in Chrome and Edge) or **Expand All** (in Firefox and Safari).</Figcaption>

<br/><br/><br/>
## Search by Selector
<Figcaption src={img7} alt="Search by selector">Use the search bar in the Inspector to find elements by their CSS selectors.</Figcaption>

<br/><br/><br/>
## Scroll Element into View
<Figcaption src={img8} alt="Scroll an element into view">Quickly scroll an element into view by right-clicking and selecting the option to bring it into focus.</Figcaption>

<br/><br/><br/>
## Drag and Drop Nodes
<Figcaption src={img9} alt="Drag and drop nodes in the DOM">Rearrange elements in the DOM tree by clicking and dragging them to your desired position.</Figcaption>

---

**That's all for now!**

These tips can help you navigate DevTools more effectively, saving you time and effort while developing your web applications. Happy coding!
